(function ($) {

    'use strict';
	
    // ------------------------------------------------------- //
    // Datepicker
    // ------------------------------------------------------ //	
	$(function () {
		//default date range picker
		$('#date24').datepicker({
	      autoclose: true,
	      format : 'yyyy-mm-dd',      
	    });

	    $('#date25').datepicker({
	      autoclose: true,
	      format : 'yyyy-mm-dd',      
	    });

	    $('#date26').datepicker({
	      autoclose: true,
	      format : 'yyyy-mm-dd',      
	    });

	    $('#date27').datepicker({
	      autoclose: true,
	      format : 'yyyy-mm-dd',      
	    });

	    $('#date28').datepicker({
	      autoclose: true,
	      format : 'yyyy-mm-dd',      
	    });

	    $('#date29').datepicker({
	      autoclose: true,
	      format : 'yyyy-mm-dd',      
	    });

	    $('#date30').datepicker({
	      autoclose: true,
	      format : 'yyyy-mm-dd',      
	    });

	    $('#date31').datepicker({
	      autoclose: true,
	      format : 'yyyy-mm-dd',      
	    });

	    $('#date32').datepicker({
	      autoclose: true,
	      format : 'yyyy-mm-dd',      
	    });

	    $('#date33').datepicker({
	      autoclose: true,
	      format : 'yyyy-mm-dd',      
	    });		

	    $('#date34').datepicker({
	      autoclose: true,
	      format : 'yyyy-mm-dd',      
	    });

	    $('#date35').datepicker({
	      autoclose: true,
	      format : 'yyyy-mm-dd',      
	    });

	    $('#date36').datepicker({
	      autoclose: true,
	      format : 'yyyy-mm-dd',      
	    });

	     $('#date37').datepicker({
	      autoclose: true,
	      format : 'yyyy-mm-dd',      
	    });

	      $('#date38').datepicker({
	      autoclose: true,
	      format : 'yyyy-mm-dd',      
	    });

	    $('#date39').datepicker({
	      autoclose: true,
	      format : 'yyyy-mm-dd',      
	    });

       $('#date40').datepicker({
	      autoclose: true,
	      format : 'yyyy-mm-dd',      
	    });   

		$('#date41').datepicker({
	      autoclose: true,
	      format : 'yyyy-mm-dd',      
	    });

		$('#date42').datepicker({
	      autoclose: true,
	      format : 'yyyy-mm-dd',      
	    });

	    $('#date43').datepicker({
	      autoclose: true,
	      format : 'yyyy-mm-dd',      
	    });

	    $('#date44').datepicker({
	      autoclose: true,
	      format : 'yyyy-mm-dd',      
	    });

	    $('#date45').datepicker({
	      autoclose: true,
	      format : 'yyyy-mm-dd',      
	    });

	    $('#date46').datepicker({
	      autoclose: true,
	      format : 'yyyy-mm-dd',      
	    });	    

	    $('#date47').datepicker({
	      autoclose: true,
	      format : 'yyyy-mm-dd',      
	    });	    

	    $('#date48').datepicker({
	      autoclose: true,
	      format : 'yyyy-mm-dd',      
	    });	    
	})
		
})(jQuery);